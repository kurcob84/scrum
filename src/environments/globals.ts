/**
 * defines constants for whole project
 * 
 */
export const CONST = Object.freeze({

    /**
     *  defines storage path/name of JWT token in locastorage
     */ 
    LOCALSTORAGE_TOKEN_PATH  : 'SCRUM_AUTHORIZATION_TOKEN',

    /**
     *  defines field of HTTP-Header where JWT-token will be injected
     */ 
    JWT_AUTH_HEADER : 'Authorization',
});
  
